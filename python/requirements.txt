# NeoFL gateway has a zero-dependency core. Optional integrations may add packages here.
# Supabase persistence uses the standard-library HTTPS client so the gateway can boot
# without installing a third-party SDK.
MetaTrader5>=5.0.45
